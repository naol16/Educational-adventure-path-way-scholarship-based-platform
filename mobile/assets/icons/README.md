Place your launcher icon image here as a square PNG named `app_icon.png`.

Recommended size: 1024x1024.

Once the file is added, run:

flutter pub get
flutter pub run flutter_launcher_icons:main

This will generate the Android and iOS launcher icons from `assets/icons/app_icon.png`.
